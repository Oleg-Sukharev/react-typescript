const numbers = [1, 2, 3, 4];

const strs: string[] = [];
const strs2: Array<string> = [];
strs.push("string");

const cars: UserBase[] = [];
cars.push({ name: "Hero", age: NaN });

const arrOfArr: string[][] = [];
arrOfArr.push(["", ""]);
